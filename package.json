{
  "name": "idelsoft-sdet-playwright-challenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "pnpm exec playwright test",
    "test:ui": "pnpm exec playwright test --grep @ui",
    "test:api": "pnpm exec playwright test --grep @api",
    "test:integration": "pnpm exec playwright test --grep @integration",
    "test:smoke": "pnpm exec playwright test --grep @smoke"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.3"
  },
  "dependencies": {
    "@faker-js/faker": "^10.1.0",
    "dotenv": "^17.2.3"
  }
}
